/** Custom display font applied to all screen headers and the wordmark. */
export const Fonts = {
  /** Wordmark / hero display — PERGA logotype */
  display: "Figtree_800ExtraBold",
  /** Screen titles, auth headings */
  heading: "Figtree_700Bold",
} as const;
